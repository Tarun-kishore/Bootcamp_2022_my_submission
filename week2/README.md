To run the files first run `make` command in shell.

Two executable with name `client` and `server` will be created.

Run server by `./server portnumber`
Run clients by `./client hostname portnumber`.
